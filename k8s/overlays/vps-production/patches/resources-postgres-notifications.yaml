apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-notifications
spec:
  template:
    spec:
      containers:
        - name: postgres-notifications
          resources:
            requests:
              cpu: 75m
              memory: 192Mi
            limits:
              cpu: 250m
              memory: 384Mi
