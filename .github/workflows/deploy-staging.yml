name: Deploy Staging

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Tag das imagens a aplicar no staging"
        required: true
        default: staging
        type: string
      registry_image_prefix:
        description: "Prefixo opcional do registry, ex.: ghcr.io/org"
        required: false
        default: ""
        type: string

permissions:
  contents: read

concurrency:
  group: deploy-staging
  cancel-in-progress: false

jobs:
  deploy:
    name: Apply Staging Overlay
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: v1.30.0

      - name: Configure kubeconfig
        env:
          KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_STAGING }}
        run: |
          set -euo pipefail
          mkdir -p "$HOME/.kube"
          if printf '%s' "$KUBE_CONFIG_DATA" | base64 --decode > "$HOME/.kube/config" 2>/dev/null; then
            :
          else
            printf '%s' "$KUBE_CONFIG_DATA" > "$HOME/.kube/config"
          fi
          chmod 600 "$HOME/.kube/config"

      - name: Validate cluster context
        run: |
          kubectl config current-context
          kubectl cluster-info

      - name: Prepare staging overlay
        env:
          REPOSITORY_OWNER: ${{ github.repository_owner }}
          INPUT_REGISTRY_IMAGE_PREFIX: ${{ inputs.registry_image_prefix }}
          REGISTRY_IMAGE_PREFIX_VAR: ${{ vars.REGISTRY_IMAGE_PREFIX }}
          STAGING_API_GATEWAY_URL_VAR: ${{ vars.STAGING_API_GATEWAY_URL }}
          STAGING_FRONTEND_URL_VAR: ${{ vars.STAGING_FRONTEND_URL }}
          IMAGE_TAG: ${{ inputs.image_tag }}
        run: |
          set -euo pipefail

          workspace="/tmp/medsync-k8s"
          rm -rf "$workspace"
          cp -R k8s "$workspace"

          overlay_dir="$workspace/overlays/staging"

          image_prefix="${INPUT_REGISTRY_IMAGE_PREFIX:-}"
          if [ -z "$image_prefix" ]; then
            image_prefix="${REGISTRY_IMAGE_PREFIX_VAR:-}"
          fi
          if [ -z "$image_prefix" ]; then
            owner="$(echo "${REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')"
            image_prefix="ghcr.io/${owner}"
          fi
          image_prefix="${image_prefix%/}"

          sed -i "s#ghcr.io/your-user#${image_prefix}#g" "$overlay_dir/kustomization.yaml"
          sed -i "s/newTag: staging/newTag: ${IMAGE_TAG}/g" "$overlay_dir/kustomization.yaml"

          api_gateway_url="${STAGING_API_GATEWAY_URL_VAR:-}"
          if [ -n "$api_gateway_url" ]; then
            api_gateway_host="${api_gateway_url#http://}"
            api_gateway_host="${api_gateway_host#https://}"
            sed -i "s#api.staging.medsync.local#${api_gateway_host}#g" "$overlay_dir/patches/ingress-api-gateway.yaml"
          fi

          frontend_url="${STAGING_FRONTEND_URL_VAR:-}"
          if [ -n "$frontend_url" ]; then
            sed -i "s#http://frontend.staging.medsync.local#${frontend_url}#g" "$overlay_dir/patches/platform-config.yaml"
          fi

          kubectl kustomize "$overlay_dir" > /tmp/medsync-staging-rendered.yaml

      - name: Apply staging manifests
        run: kubectl apply -k /tmp/medsync-k8s/overlays/staging

      - name: Validate rollouts
        run: |
          set -euo pipefail
          for deployment in api-gateway frontend auth-service users-service patients-service triage-service medical-record-service notifications-service; do
            kubectl rollout status "deployment/${deployment}" -n medsync-staging --timeout=180s
          done

      - name: List staging pods
        run: kubectl get pods -n medsync-staging -o wide
